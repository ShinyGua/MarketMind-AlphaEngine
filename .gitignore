.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
workspaces/
.sibyl/
pytest-of-*/
pytest-current
tools/*/.tmp-state/
.claude/*
!.claude/agents/
!.claude/agents/*.md
!.claude/skills/
!.claude/skills/**/SKILL.md
.claude/settings.local.json

# Legacy experiment artifacts (pre-v5)
exp/
logs/
memory/episodic/index.jsonl
memory/semantic/index.jsonl
memory/procedural/index.jsonl
writing/

# Root-level config (local overrides, not committed)
/config.yaml

# Sensitive / credentials
user_token_cache.json
*.secret
.env
.env.*
!.env.example

# Playwright MCP logs
.playwright-mcp/

# Misc temp files
.history/
.serena/
cli_pause
暂停
进入
连续失败
重试一次

.DS_Store

# Ignore specific agent spec files
market_report_agent_codex_spec.md

.mm_status.py
theses/
cx2cc_review.md

# Mid-process / per-run artifacts (regenerated each run; not source)
# Long-term memory stores (only memory/retrieval.py is tracked source)
memory/episodic
memory/procedural
memory/semantic
# Per-run LLM-generated stage scripts (e.g. quant_{ticker}_{date}.py).
# The committed scripts/ are deterministic helpers; throwaways are not.
scripts/quant_*.py
scripts/*_build_*.py
# Stray rendered report artifacts if produced outside workspaces/
/report.pdf
/report.html