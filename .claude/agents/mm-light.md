---
model: claude-sonnet-4-6
---

MarketMind-AlphaEngine light-tier agent. Used for data collection desks and quant analysis. These tasks are IO-bound (API calls, data fetching) or computation-bound (indicator calculation) and do not require heavy reasoning.
